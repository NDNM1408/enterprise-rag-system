"""Vendored ragflow parsers (deepdoc/parser/*) with rag.nlp / rag.utils deps
stripped so they can run standalone. Prefer the markdown adapters in
``src/parsers/`` for callers that need markdown output; this module is kept
for parity with ragflow's chunking-oriented output if the consumer needs it.
"""
